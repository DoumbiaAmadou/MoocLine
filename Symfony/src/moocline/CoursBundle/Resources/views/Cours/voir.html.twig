{# empty Twig template #}
{# On étend le layout principal #}
{% extends "::layout.html.twig" %}

{% block title %} 
  {{ parent() }} - Cours
{% endblock %}

{% block body %}
    
    <div class="container-fluid well">
        
         <div class="panel panel-default">
            <div class="span4 offset1">
            {{cours.presentation|raw }}
            </div>
        </div>
    </div>
   <form class="form-horizontal" method="POST" action="">    
        <div class="col-md-2 col-md-offset-3">
            <a href="{{ path('moocline_cours_edition', {'id':cours.id})}}" class="btn btn-primary btn-block">Modifier</a>
        </div>
        <div class="col-md-2">
            <a href="{{ path('moocline_cours_chapitre')}}" class="btn btn-primary btn-block">créer chapitre</a>
        </div>
        <div class="col-md-2">
            <a href="{{ path('dashboard_Enseignant', {'id':app.user.id})}}" class="btn btn-primary btn-block">retour</a>
        </div> 
   </form>
        <br><br>
    
    
 
 {% endblock %}